# DESIGN.md — 设计准则与体验规范（2026-07-10）

> 本文件是全站 UI、视觉与交互决策的唯一依据。两大支柱是 **舞台剧（Theatre）** 与 **Liquid Glass**。
>
> **内容是舞台，界面是玻璃。** 视频、鹿、照片、研究图表保持电影素材的直接质感；只有观看行为所需的按钮、边框、展柜和提示属于玻璃。玻璃是观众与记忆之间的观剧之镜，不属于剧情本身。

## 1. Liquid Glass 材质系统

### 1.1 五层材质

1. **背景折射**：共享 `#lg-refract` 滤镜，以轻微位移模拟曲面镜片；不支持时自然回退为磨砂。
2. **厚度**：外投影、内顶高光与内底暗部共同形成薄玻璃边缘。
3. **亮边**：遮罩渐变描边，顶部最亮，侧边消隐，底部留微弱反光。
4. **湿润高光**：负 spread 内阴影形成薄而克制的湿润亮边。
5. **交互光**：光标跟随的白色镜面高光与细微光谱色散；只在可交互表面 hover 时加强。

Hover 物理：`translateY(-1px) scale(1.02)`，ease-out-expo，0.4 秒，无弹跳。`prefers-reduced-motion` 不进入电影模式，因此不依赖局部关闭动画来“勉强合规”。

### 1.2 组件

- `GlassSurface`：非语义的被动材质容器；`frost` 控制低成本磨砂，`hover` 控制交互光。
- `GlassButton`：原生 `button`，用于 Scroll、Replay 等动作。
- `GlassLink`：原生 `a`，用于 GitHub 等外链。
- `GlassFilterDefs`：全站只挂载一次的折射滤镜定义。
- `AccessibleDialog`：通过 portal 置于全站顶层，统一处理标题、关闭按钮、Escape、焦点圈定、焦点恢复与背景滚动锁定。

图标只来自 `lucide-react` 或已有正式图标素材，不手绘近似图标。

### 1.3 使用红线

- 玻璃只用于界面；剧情内容永远不是玻璃。
- 同屏真折射面不超过 2 个；大面板使用 `frost`。
- 3D `preserve-3d` 内禁用 `backdrop-filter`，S5 卡片只用边缘切片与 `.glass-rim` 模拟厚板。
- 玻璃自身无色；颜色只能来自场景灯光透过玻璃。
- 可点击外观必须由原生 `button` 或 `a` 承载，不允许 clickable `div`。

## 2. 舞台系统

1. **镜框（Proscenium）**：常驻上下窄遮幅，把每一幕框进台口。
2. **地沿（Stage Apron）**：底部黑色渐变是小鹿的家，保证亮景下仍有稳定落脚点。
3. **聚光（Spotlight）**：共享冷白、琥珀或蓝白光池；尘埃只服务于空间深度。
4. **换灯转场**：场景边界使用短暂 dip-to-black；S0 结尾保留硬切黑。
5. **幕间字卡**：Spectral Light italic，大句整句淡入，不逐词炫技。
6. **姿态**：所有运动克制、可读、ease-out-expo、无弹跳。

鹿在桌面电影中是一个独立、持续的 overlay：S0 由世界视频内的鹿开场，随后交给地沿上的小鹿，终幕再扩展为全幅英雄镜头。任何场景 lightbox 都通过 portal 盖过鹿，避免前后景穿帮。

## 3. 文本系统

| 角色 | 字体 | 用途 |
|---|---|---|
| Display / Intertitle | Spectral Light italic 300 | 幕间句、回忆句、终幕签名 |
| Narrative | Hanken Grotesk 350/400/500 | 旁白、正文、动作标签 |
| Meta | JetBrains Mono 400/500 | 作品媒介、年份、研究状态与数据 |
| 中文 | Noto Sans SC 400/500/700 | 微信幕及其中文原文 |

正文以 `white/74–90` 为主；小号 mono 元数据不得低于 `white/60`。视频上叙事文字必须同时使用安全区与 `text-scrim`。

## 4. 每幕灯光

| 幕 | 灯光与材质 |
|---|---|
| S0 序幕 | 夜薰衣草紫 × 暖金，近黑底 |
| S1 提问 | 纯黑 + 冷白聚光；微信绿只作为道具色 |
| S2 入境 | 素材日光原色，由遮幅与地沿压入舞台 |
| S3 此刻 | 放映琥珀 + 尘埃；真实家庭档案照片作为银幕 |
| S4 回忆 | 钴蓝 moving image 转为桌面琥珀；真实拍立得组成记忆档案 |
| S5 摄影 | 冷月光蓝灰展廊；真实电影静帧置于 3D 玻璃厚板 |
| S6 研究 | 黑场 + 两束展柜追光；真实研究图保持白底原貌 |
| S7 终幕 | 纯黑、星尘与鹿的自体发光；只保留 GitHub / Email / Replay |

## 5. 两种观看模式

### 5.1 Cinematic mode

- 适用：CSS viewport ≥1024px 且未请求 reduced motion。
- 无持久导航；滚动就是剪辑台。
- 除终幕 A3 在进入后自动完整播放外，其余剧情视频以 `currentTime` 绑定滚动。
- 开屏键盘焦点可进入“Read this story without motion”，但视觉上保持隐藏，避免破坏电影画面。
- 场景淡出后同时停止 pointer interaction，并从辅助技术阅读顺序中隐藏。

### 5.2 Reading mode

- 适用：viewport ≤1023px、200% 缩放导致的紧凑布局，或 `prefers-reduced-motion: reduce`。
- 不是降级提示，而是完整的八幕线性叙事。
- 使用真实静帧、家庭照片与研究图；不挂载任何 `<video>`，避免移动端承担 4K 素材成本。
- 中文对话保留原文并提供可见英文翻译。
- 画面仍使用舞台黑、真实内容与克制的玻璃动作，不变成通用作品卡片站。

## 6. S5 视觉研究轮播

### 6.1 运动机制

- rAF 渲染循环，`perspective: 1350px`，`preserve-3d`，卡片绝对居中。
- `progress` 与滚动绑定，在线性五件作品之间移动，不循环 wrap。
- 磁性驻留：`rounded = round(p)`；中心附近以四次幂减速，形成“停一拍再离开”的节奏。
- 三段 smoothstep 轨迹：中心 → 相邻 → 屏缘 → 完全出屏。
- 中心卡随光标产生最多约 15° / 12° 的惯性倾斜，离中心越远倾斜越弱。
- 每张卡由五个 z 切片形成约 3px 玻璃厚度；3D 内不使用真折射。
- 只有中心卡可点击、可聚焦、进入辅助技术阅读顺序。

### 6.2 内容与交互

五件作品全部来自本故事的真实 moving-image 静帧：`Stage in Blue`、`Overture`、`Arrival`、`The Guide`、`Curtain`。

- 正面：真实图像、细玻璃 rim、顶部柔和 sheen。
- 背面：同图模糊背景 + 标题 / 媒介 / 年份。
- 打开：顶层无障碍 dialog；焦点首先进入关闭按钮。
- 翻面：放大的图像本身是原生 button，`aria-pressed` 表示状态。
- 提示文案：`scroll to browse · click to look closer`。

## 7. 内容真实性与素材策略

- 家庭档案照片可裁切、压缩、调色，但不得替换为 stock 或生成的“相似童年”。
- 未知年份明确写 `year unknown`；不编造城市、器材或项目状态。
- S3、S4 与 Reading mode 共享同一真实家庭档案。
- 研究图只使用 Ethan 自己的结果图；BOLD5000 刺激图片不公开展示。
- 原始母版保留在制作目录；公开投放版按实际展示尺寸与交互目的编码。滚动控制视频使用 H.264 MP4、fast-start 与短 GOP，并提供高清 poster；首幕只在静帧、字体、首帧和约 2.5 秒连续缓冲可用后开放滚动，后续素材按前一幕分阶段预取。小鹿 run / idle 使用适配 12vh 展示的 720p H.264；A3 保留 4K HEVC HDR，并提供单独选择的 1080p H.264 SDR fallback。
- 图标使用成熟图标库；缺少真实视觉素材时优先等待原素材或调用正式图像生成流程，不用 CSS / SVG 假画占位。

## 8. 已决事项与开放边界

已决：

- “Ethan Gan” 只在终幕正式出现；Reading mode 顶部以小号署名帮助页面识别。
- 不增加幕名导航；Reading mode 可显示 Act 标签以支撑线性阅读。
- S5 元数据字段固定为标题 / 媒介 / 年份。
- S2 保留强电影遮幅。
- 终幕提供 GitHub、Email 与 Replay；没有真实 CV 前不添加 CV 按钮。

开放边界：

- 音频仍不在当前范围。
- 公开部署前必须对家庭成员照片做一次明确的隐私确认。
- 若未来补充新的摄影作品，必须同时提供真实标题、媒介、年份和可访问替代文本。
